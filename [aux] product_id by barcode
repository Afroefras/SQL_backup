select * from 
    (select 
        unnest(p.barcodes) as barcodes,
        p.id as product_id
    from catalog_product p
    where currency = 'MXN' and p.is_active
    ) as a
where barcodes in (
    '7501032908331'
)
