select
    string_agg(distinct sb.id::text, ',') as branches
from catalog_storebranch sb
    join catalog_store s on s.id = sb.store_id
where 1=1
    and s.id = 25
    and sb.city_id = 3
    and sb.active
